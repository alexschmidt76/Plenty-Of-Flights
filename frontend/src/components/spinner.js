import loading_spinner from '../Loading.gif'

const Spinner = () => {
    return (
        <div>
            <img src={loading_spinner} alt="loading..." />
        </div>
    )
}

export default Spinner